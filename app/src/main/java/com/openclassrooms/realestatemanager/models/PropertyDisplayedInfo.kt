package com.openclassrooms.realestatemanager.models

data class PropertyDisplayedInfo (val id: Long,
                                  val area: Double,
                                  val status: Boolean)